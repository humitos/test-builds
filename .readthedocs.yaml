version: 2

build:
  os: ubuntu-lts-latest  # triggering by commit message -- try again 2
  tools:
    python: latest

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: requirements.txt
