version: 2

build:
  os: ubuntu-lts-latest  # adding a comment here to test pushing to a PR
  tools:
    python: latest

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: requirements.txt
